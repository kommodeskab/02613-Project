# Setup Instructions

## 1. Create the environment

```bash
module load python3/3.10
python3 -m venv .venv
pip install -r requirements.txt
```

## 2. Before starting coding

Each time before you start coding, run:

```bash
voltash
module load python3/3.10
source .venv/bin/activate
```
